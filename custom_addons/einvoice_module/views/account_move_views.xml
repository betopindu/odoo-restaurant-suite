<odoo>
    <record id="view_move_form_fiscal_documents" model="ir.ui.view">
        <field name="name">account.move.form.fiscal.documents</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_fiscal_documents"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-file-text-o"
                        invisible="fiscal_document_count == 0">
                    <field name="fiscal_document_count" widget="statinfo" string="Fiscal Documents"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
